/**
 * Single source of truth for company identifiers shown in legal / marketing copy.
 * Replace TODO values before going to production.
 *
 * isProjectInProgress: when true, the app shows a site-wide notice that the product
 * and business are not final; set to false when registered and you are ready for
 * public commercial use, then complete legal text and C2 (e.g. address, VAT).
 */
export const COMPANY_INFO = {
  orgNumber: 'TODO_ORG_NUMBER',
  email: 'TODO_EMAIL',
  isProjectInProgress: true,
}
